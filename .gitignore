# Compiled binaries for various OS architectures
# These are typically generated by `go build` in the current directory
*.exe
*.dll
*.so
*.dylib

# Compiled binaries generated by `go install` (if you use it)
# These usually go into the GOPATH/bin directory, but sometimes a local 'bin' folder
bin/

# Dependency directories
# If you are using Go Modules (Go 1.11+), `vendor/` is where vendored dependencies are stored.
# If you are not vendoring, you might not need this, but it's good to include for safety.
vendor/

# Test binary, generated with `go test -c`
*.test

# Output of coverage tests, generated with `go test -coverprofile`
*.out

# Go module cache and download directories
# These are typically managed by Go itself and shouldn't be committed
.cache/
pkg/

# IDE-specific files and directories
# IntelliJ IDEA / GoLand
.idea/

# Visual Studio Code
.vscode/

# Eclipse (if used for Go development)
.project
.classpath
.settings/

# macOS specific files
.DS_Store

# Windows specific files
Thumbs.db

# Environment variables files (e.g., for local development)
.env
.env.*

# Log files
*.log
logs/

# Temporary files
*.tmp