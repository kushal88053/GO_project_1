{
    "functions": {
        "api/index.go": {
            "runtime": "go"
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/index.go"
        }
    ]
}